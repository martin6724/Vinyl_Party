<p id="notice"><%= notice %></p>

<h1>My Collection</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Image</th>
      <th>Listen on Youtube</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @albums.each do |album| %>
      <tr>
        <td><%= album.title %></td>
        <td><%= album.image  %></td>
        <td><%= album.url %></td>
        <td><%= link_to 'Show', users_album_path(album) %></td>
        <td><%#= link_to 'Edit', edit_method-url, remote: true %></td>
        <td><%#= link_to 'Destroy', album, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Album', new_users_album_path %>
